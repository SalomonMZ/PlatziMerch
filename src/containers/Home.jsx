import React from 'react';
import initialState from '../initalState'
import Products from '../components/Products'

const Home = () => {
    return (
        <Products/>
    );
}

export default Home;
